({
	cancel : function(component, event, helper) {
		helper.cancel(component, event);
	},
    changePasswordButton : function(component, event, helper) {
        helper.changePasswordButton(component, event);
    }
})